# 홈페이지 빌더 사용 안내

이 문서는 **운영하는 쪽**을 위한 것입니다. 실제 쓰는 방법은 도구 안에 들어 있습니다.

## 먼저 — 설명은 화면 안에 있습니다

`/builder` 를 여시면 오른쪽 위에 세 가지가 있습니다.

| 단추 | 하는 일 |
| --- | --- |
| **▸ 따라 하기** | 화면의 그 자리를 짚어 주며 열 걸음으로 직접 해 봅니다. 처음 열면 저절로 시작됩니다. |
| **? 사용법** | 전체 매뉴얼 8장. 차례를 눌러 이동하고, 인쇄도 됩니다. |
| 메뉴 옆 작은 **?** | 그 메뉴만 설명하는 쪽지. 13곳에 있습니다. |

고객에게 건네실 때는 「따라 하기」만 알려 주시면 됩니다. 읽는 것보다 빠릅니다.

---

## 1. 30분 만에 사이트 한 벌 만들기

1. **템플릿을 고릅니다.** 위쪽 「템플릿」 → 여섯 가지 중 고객에게 가까운 것.
   - TEN AI 소개 / AI 교육 과정 / AX 컨설팅 / 중소기업 회사 소개 / 공공기관 사업 안내 / 빈 문서
2. **글자를 고칩니다.** 캔버스의 글자를 직접 누르면 커서가 생깁니다.
   `Enter` 확정, `Esc` 되돌리기.
3. **뼈대를 고릅니다.** 왼쪽 「레이아웃」 → 기본형·박스형·사이드 메뉴형·센터형·분할형·포털형.
   내용은 그대로 두고 골격만 바뀌므로 고객 앞에서 여섯 개를 넘겨 보이면 됩니다.
4. **색을 맞춥니다.** 왼쪽 「테마」 → 견본 다섯 가지. 고객 로고 색이 있으면
   강조색만 직접 지정하셔도 충분합니다.
5. **필요 없는 구역을 숨기고, 필요한 구역을 넣습니다.** 「구역」에서 ● 로 숨기고,
   「추가」에서 게시판·FAQ·문의 폼·오시는 길·동영상을 넣습니다. 사진이 없으면 「샘플 사진」·「삽화」로
   자리를 채우고, 카드에는 아이콘을 답니다.
6. **PC·태블릿·모바일을 확인합니다.** 위쪽 기기 단추로 넘겨 봅니다. 모바일에서는 메뉴가 ☰ 로 접힙니다.
7. **내보내거나 퍼블리시합니다.** 「HTML 내보내기」 → 파일 하나. 올리는 것까지 맡기려면
   「퍼블리시 → 배포 꾸러미」를 받아 AI 에이전트에게 건넵니다(아래 6장).

## 2. 작업을 이어서 하려면

- 작업물은 **브라우저에 자동으로 저장**됩니다. 같은 브라우저에서 다시 열면 그대로입니다.
- 다른 기기로 옮기거나 보관하시려면 「저장(JSON)」 으로 파일을 내려받고,
  옮긴 곳에서 「불러오기」로 되읽으십시오. 내보낸 HTML 파일도 「불러오기」에 넣으면
  다시 열립니다(작업 문서가 안에 심겨 있습니다).
- **다른 브라우저·다른 PC 에서는 이어지지 않습니다.** 서버에 두지 않기 때문입니다.
  고객 작업물은 JSON 으로 받아 보관하시는 편이 안전합니다.

## 3. 내보낸 파일을 올리는 곳

HTML 한 장이므로 거의 어디에나 올라갑니다.

| 방법 | 언제 |
| --- | --- |
| 고객의 기존 웹호스팅에 파일 복사 | 이미 도메인이 있을 때 |
| Vercel·Netlify 에 폴더째 끌어놓기 | 새 주소가 필요할 때 (무료) |
| 메일·메신저로 파일 전달 | 시안을 먼저 보여 드릴 때 |

이미지는 파일 안에 함께 담기므로 사진이 깨지지 않습니다. 대신 사진을 많이 넣으면
파일이 커집니다 — 시안 단계에서는 2~3장으로 충분합니다.

## 4. 게시판을 넣었을 때

게시판 구역을 넣으면 내보낸 HTML 안에서 **글 목록·읽기·글쓰기·말머리 고르기·검색·쪽 나누기가
그대로 돕니다.** 다만 서버가 없으므로 방문자가 새로 쓴 글은 **그 사람의 브라우저에만** 남습니다.

- 공지·안내·자료처럼 **미리 넣어 두는 글**은 빌더에서 넣으십시오. 모두에게 보입니다.
  자료실이면 글의 「첨부·링크」에 파일 주소를 넣습니다.
- 소식·블로그처럼 보이게 하려면 「모양」을 카드형으로 바꿉니다. 한 사이트에 게시판을 여러 개 두어도 됩니다.
- 방문자끼리 글을 주고받는 진짜 게시판이 필요하면 서버가 있어야 합니다.
  그때는 텐에이아이에 문의하십시오.

## 5. 문의 폼 · 오시는 길 · 동영상

- **문의 폼**: 「받을 주소」를 비우면 방문자의 메일 프로그램이 열려 적어 둔 이메일로 보내집니다.
  서버에서 받으려면 Formspree·Getform 같은 서비스 주소를 넣으십시오(무료 요금제로 충분합니다).
- **오시는 길**: 주소만 적으면 지도가 섭니다. API 키가 필요 없습니다.
- **동영상**: 유튜브 주소를 어느 형태로 붙여 넣어도 됩니다.

## 6. AI 에이전트에게 올리기를 맡길 때

「퍼블리시 → 배포 꾸러미 내려받기」를 누르면 ZIP 하나가 떨어집니다. 안에는
`index.html`(사이트) · `site.json`(작업 문서) · `manifest.json`(구조 요약) · `PUBLISH.md`(배포 지시문) ·
`vercel.json` 이 있습니다.

1. ZIP 을 풀어 폴더째 Claude Code(또는 다른 코딩 에이전트)에게 건넵니다.
2. 「퍼블리시 → 지시문 복사」로 복사한 한 문단을 붙여 넣거나, 그냥 "이 폴더를 Vercel 에 배포해줘" 라고 합니다.
3. 에이전트는 `PUBLISH.md` 를 읽고 빌드 없이 그대로 올립니다. 내용을 고치면 `site.json` 도 함께 고치므로
   나중에 빌더에서 다시 열 수 있습니다.

자체 서버나 자동화 도구(n8n·Make)로 받으시려면 「설정 → 연동 주소」에 웹훅 주소를 적고
「퍼블리시 → 지금 보내기」를 누르십시오. 같은 파일들이 JSON 으로 POST 됩니다.

## 7. 고객에게 설명할 때 쓰는 말

> "지금 보시는 이 화면이 그대로 결과물입니다. 마음에 안 드는 글자를 짚어 주시면
> 그 자리에서 고쳐 보여 드리겠습니다."

빌더의 값은 **고치는 속도**에 있습니다. 회의 중에 고쳐서 바로 보여 드리는 것이
시안을 세 번 주고받는 것보다 낫습니다.

---

운영·배포·브랜드 교체는 [README.md](./README.md) 를 보십시오.
문의: leesh@tenai.kr
